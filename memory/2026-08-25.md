# 2026-08-25 满分虎项目工作记录

## 会话：成绩测评工具三项修改 + Blob 持久化

### 1. 成绩测评 · 输入区参考成绩（t1）
- 文件：`youth-fitness-website/public/tools/assessment.html`
- 每个学科输入框下方新增参考行：满分成绩 / 50分成绩 / 最低成绩（数据来自 `assessment_data.js` 的 `ASSESS_DATA.guoti.tables` / `xianchang.tables`）
- BMI 特殊处理：分档合并显示（正常 14.2-20.1 · 80分 ≤14.1/20.2-22.6 · 60分 ≥22.7）；manual 型显示"手动评分 0-6 分"；time 类转分:秒

### 2. 成绩测评 · 缺录按 0 分计（t2）
- `computeGuoti`：分母改为全年级权重总和（恒 100），未填项 missing++ 按 0 分计权重；全未填返回 null
- `computeXianchang`：totalMax 恒 30（8+8+8+6），缺录类别计 0 分；全缺返回 null
- 结果头 rhSub 追加"· 缺录 M 项按 0 分计"

### 3. 国体满分 120（用户新需求）
- 结果头/图片报告/复制报告均显示 `/120分`，副标题"满分 120（标准分100+附加分20）"
- `api/data.js` 后端钳制上限 100 → 120；前端存档 `max: 120`

### 4. UI 审计
- BMI 身高体重输入区重构：`.bmi-field`（身高 cm / 体重 kg 单位标签），参考行字号 9.5px 紧凑化

### 5. 数据持久化：启用 Vercel Blob
- `api/data.js` 接入 `@vercel/blob`：整个 store 存为 `manfenhu-store.json`，写操作原子落盘、读请求实时拉取，无 token fallback 内存+种子
- 诊断：`/api/data?diag=1` → `backend: "blob+seed"`、`persistent: true`
- 成本：数据几 KB~几十 KB，免费额度内 ≈ 0 元/月

### ⚠️ 安全提醒（已告知用户）
- `quick-start.bat` 第 59 行**硬编码了 VERCEL_TOKEN**——交接文档明确要求 token 不入库，建议尽快轮换该 token 并改为环境变量

### 文档
- `项目交接文档.md` 更新至 v1.1：新增 120分上限逻辑、缺录0分、Blob 持久化、更新日志

### 待办
- 用户测试后反馈，可能需要微调
- 建议 git commit（未 push）