# 满分虎学员档案系统 - 项目交接文档

**项目名称**: 满分虎学员档案系统  
**交接日期**: 2026-08-22  
**版本**: v1.0  
**线上地址**: https://www.manfenhu.com

---

## 📦 项目概述

满分虎学员档案系统是一套完整的教学管理工具集，包含学员档案管理、成绩测评、课后总结生成、群话术海报生成等核心功能，专为青少年体能教练和班主任设计。

### 核心功能

1. **学员档案管理**
   - 学员信息录入与管理
   - 测评记录追踪
   - 7维度雷达图可视化
   - 批量导出 & 单人导出

2. **成绩测评工具**
   - 国体测评（4/6/8年级）
   - 现场考试评分（22个项目）
   - 自动评分与等级判定
   - BMI计算与体态评估

3. **课后总结生成器**
   - 28项表现维度
   - 10档描述智能生成
   - 性别过滤
   - 一键复制分享

4. **群话术海报工具**
   - 节气/节日/天气问候
   - 品牌句自动植入
   - AI背景图生成
   - 一键保存海报

5. **统一工具导航**
   - 所有工具一键访问
   - 品牌色卡片式设计
   - 移动端完美适配

---

## 🗂️ 项目结构

```
manfenhu-website/
├── youth-fitness-website/          # 主项目目录
│   ├── api/                        # API 接口
│   │   ├── data.js                 # 学员数据 API
│   │   └── seed-data.json          # 数据种子文件
│   ├── public/                     # 前端页面
│   │   ├── tools/                  # 工具集
│   │   │   ├── index.html          # 🆕 工具导航页
│   │   │   ├── assessment.html     # 成绩测评
│   │   │   ├── summary.html        # 课后总结
│   │   │   └── group.html          # 群话术海报
│   │   ├── students.html           # 学员列表
│   │   ├── student-detail.html     # 学员详情
│   │   └── images/                 # 图片资源
│   ├── package.json                # 依赖配置
│   └── vercel.json                 # 部署配置
├── backups/                        # 备份目录
│   └── ui-polish-backup-20260822/  # UI优化前备份
└── .git/                           # Git 仓库
```

---

## 🔧 技术栈

- **前端**: HTML5 + CSS3 + Vanilla JavaScript
- **API**: Vercel Serverless Functions (Node.js)
- **部署**: Vercel (自动部署)
- **存储**: 内存 + 种子文件 (临时方案)
- **版本控制**: Git + GitHub

---

## 🚀 部署指南

### 环境要求

- Node.js 16+
- npm 或 yarn
- Git
- Vercel CLI (可选)

### 本地开发

```bash
# 1. 克隆仓库
cd D:\manfenhu-website

# 2. 安装依赖
cd youth-fitness-website
npm install

# 3. 本地预览
npx astro dev
# 访问 http://localhost:4321
```

### 生产部署

**方式一：Vercel CLI**
```bash
# 设置 token
export VERCEL_TOKEN="你的token"

# 部署到生产
npx vercel --prod --yes
```

**方式二：Git 推送自动部署**
```bash
git add .
git commit -m "更新内容"
git push origin master

# Vercel 自动检测并部署
```

### 环境变量

当前生产环境已配置：
- `BLOB_READ_WRITE_TOKEN`: Vercel Blob 存储 token
- `BLOB_STORE_ID`: Blob store ID
- 其他遗留 KV 变量（已弃用）

**注意**: 当前使用种子文件存储，Blob 配置已完成但暂未启用。

---

## ⚠️ 重要注意事项

### 数据持久化限制

**当前状态**: 使用内存 + 种子文件存储

**影响**:
- ✅ 运行时数据正常读写
- ⚠️ 重新部署会重置数据
- ⚠️ serverless 冷启动可能丢失短期数据

**应对措施**:
1. **定期导出备份**
   - 学员列表页点击"批量导出"
   - 学员详情页点击"导出报告"
   - 建议每周导出一次

2. **重要数据手工保存**
   - 将 CSV 导出存档到本地
   - 或复制到 Excel/飞书表格

3. **长期方案**（可选）
   - 接入真正的数据库（如 Upstash Redis / Supabase）
   - 或使用 Vercel Blob（已配置环境变量，代码需调整）

### 100分限制逻辑

**场景**: 国体测评（满分100分）

**处理**: API 自动钳制
```javascript
if (record.mode === 'guoti' && record.total > 100) {
  record.total = Math.min(100, Math.round(record.total * 10) / 10);
  record.max = 100;
}
```

**位置**: `api/data.js` 第 147-150 行

### 7维度雷达图

**维度映射**:
```javascript
const DIM_MAP = {
  'bmi': '体态',
  'vital': '心肺',
  'run50': '速度',
  'sitreach': '柔韧',
  'longjump': '爆发',
  'power': '力量',
  'run800': '耐力',
  'run1000': '耐力'
};
```

**自动聚合**: 从 `items` 数组自动生成 `dimensions` 对象

---

## 📋 页面清单

| 页面 | 路径 | 功能 |
|------|------|------|
| 工具导航 | `/tools/` | 统一入口，老师只需收藏这一个 |
| 学员列表 | `/students.html` | 查看所有学员，批量导出 |
| 学员详情 | `/student-detail.html` | 单个学员档案，添加/查看记录 |
| 成绩测评 | `/tools/assessment.html` | 国体+现场评分，7维度分析 |
| 课后总结 | `/tools/summary.html` | 28项×10档智能生成 |
| 群话术海报 | `/tools/group.html` | 节气/节日/天气/品牌句海报 |

---

## 🎨 UI设计规范

### 品牌色

- **主色**: `#FF6B35` (满分虎橙)
- **辅助色**: `#FF8A4C` / `#F04E23`
- **背景渐变**: `linear-gradient(135deg, #FFF5F0 0%, #FFE8DD 100%)`

### 字体

```css
font-family: -apple-system, BlinkMacSystemFont, 
             "Segoe UI", "PingFang SC", 
             "Microsoft YaHei", sans-serif;
```

### 圆角

- 大卡片: `16-18px`
- 中按钮: `12-14px`
- 小标签: `8-10px`

### 交互

- 悬停: `translateY(-2px)` + 阴影加深
- 过渡: `transition: all 0.3s ease`

---

## 🔄 备份与回滚

### 备份位置

1. **Git Tag**: `backup-before-ui-polish`
   ```bash
   # 回滚到备份点
   git checkout backup-before-ui-polish
   ```

2. **本地备份**: `backups/ui-polish-backup-20260822/`
   - 包含所有主要页面
   - 可直接复制回 public 目录

3. **GitHub 仓库**: 完整历史记录
   ```bash
   # 查看提交历史
   git log --oneline

   # 回滚到指定提交
   git reset --hard <commit-id>
   ```

### 紧急回滚

```bash
# 1. 回到最后一次稳定提交
cd D:\manfenhu-website
git reset --hard 5efdbe1

# 2. 强制推送（慎用）
git push origin master --force

# 3. 重新部署
npx vercel --prod --yes
```

---

## 📊 数据导出格式

### 学员列表导出 (CSV)

```csv
学员ID,姓名,性别,年级,记录数,最后测评日期
s_1787418222875,张三,男,8,3,2026-08-22
```

### 单人详情导出 (CSV)

```csv
日期,年级,模式,总分,等级,BMI,肺活量,50米跑,...
2026-08-22,8,国体,94.5,优秀,96,93,95,...
```

---

## 🐛 已知问题与解决方案

### 1. 数据重置问题

**现象**: 重新部署后学员数据消失

**原因**: 当前使用内存+种子文件存储

**解决**: 
- 短期: 定期导出备份
- 长期: 接入真正的数据库

### 2. Vercel Blob 配置问题

**现象**: Blob API 初始化失败

**状态**: 环境变量已配置，代码已就绪，但当前未启用

**原因**: 初次部署遇到 `list()` / `put()` API 兼容问题

**位置**: `api/data.js` 第 20-60 行（已注释备用代码）

**解决**: 需要时可重新调试 Blob API

---

## 📞 联系方式

**项目维护**: AI Agent (Hermes/Kiro)  
**业务负责人**: 满分虎创始人  
**技术支持**: lx676 (GitHub: lx67621956-create)

---

## 📅 更新日志

### 2026-08-22 (v1.0)

**功能修复**:
- ✅ 100分越界限制（国体 >100 自动钳制为 100）
- ✅ 单项得分显示（详情页展开所有项目）
- ✅ 7维度雷达图修复（自动聚合维度数据）
- ✅ 导出功能（列表批量 + 详情单人 CSV）
- ✅ 数据持久化（种子文件方案）

**新增功能**:
- ✅ 统一工具导航页 (`/tools/`)
- ✅ 所有页面 UI 统一（品牌橙色 + 渐变背景）

**技术变更**:
- 从 Upstash KV（已失效）迁移到种子文件存储
- 添加 `@vercel/blob` 依赖（备用）
- 创建完整备份（Git tag + 本地文件）

---

## 🔐 安全注意事项

1. **API Token 保护**
   - `VERCEL_TOKEN` 不要提交到 Git
   - 仅在本地开发和 CI/CD 中使用

2. **数据隐私**
   - 学员信息仅存储在 Vercel serverless 内存
   - 导出的 CSV 文件请妥善保管

3. **环境变量管理**
   - 生产环境变量仅通过 Vercel 控制台配置
   - 不要在代码中硬编码敏感信息

---

## 🎯 未来优化方向

### 短期（1-2周）
1. 接入真正的数据库（Upstash Redis / Supabase）
2. 完善 Vercel Blob 存储（已配置环境，需调试代码）
3. 添加数据自动备份到飞书文档

### 中期（1-3个月）
1. 学员成绩趋势分析图表
2. 家长端小程序/H5 查看
3. 微信/飞书消息通知

### 长期（3个月+）
1. AI 训练计划推荐
2. 视频动作纠正
3. 多校区数据同步

---

## ✅ 交接确认清单

- [ ] 已阅读完整文档
- [ ] 已克隆代码仓库到本地
- [ ] 已验证本地开发环境可用
- [ ] 已访问线上所有页面并测试功能
- [ ] 已了解数据持久化限制
- [ ] 已知晓备份与回滚流程
- [ ] 已保存重要联系方式
- [ ] 已导出一次学员数据作为测试

---

**交接完成日期**: ____________  
**交接人签名**: ____________  
**接收人签名**: ____________

---

*本文档最后更新: 2026-08-22*  
*版本: 1.0*
