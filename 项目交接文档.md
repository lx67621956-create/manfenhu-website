# 满分虎学员档案系统 - 项目交接文档

**项目名称**: 满分虎学员档案系统  
**交接日期**: 2026-08-22  
**版本**: v1.1  
**线上地址**: https://www.manfenhu.com

---

## 📦 项目概述

满分虎学员档案系统是一套完整的教学管理工具集，包含学员档案管理、成绩测评、课后总结生成、群话术海报生成等核心功能，专为青少年体能教练和班主任设计。

### 核心功能

1. **学员档案管理**
   - 学员信息录入与管理
   - 测评记录追踪
   - 7维度雷达图可视化
   - 批量导出 & 单人导出

2. **成绩测评工具**
   - 国体测评（4/6/8年级）
   - 现场考试评分（22个项目）
   - 自动评分与等级判定
   - BMI计算与体态评估

3. **课后总结生成器**
   - 28项表现维度
   - 10档描述智能生成
   - 性别过滤
   - 一键复制分享

4. **群话术海报工具**
   - 节气/节日/天气问候
   - 品牌句自动植入
   - AI背景图生成
   - 一键保存海报

5. **统一工具导航**
   - 所有工具一键访问
   - 品牌色卡片式设计
   - 移动端完美适配

---

## 🗂️ 项目结构

```
manfenhu-website/
├── youth-fitness-website/          # 主项目目录
│   ├── api/                        # API 接口
│   │   ├── data.js                 # 学员数据 API
│   │   └── seed-data.json          # 数据种子文件
│   ├── public/                     # 前端页面
│   │   ├── tools/                  # 工具集
│   │   │   ├── index.html          # 🆕 工具导航页
│   │   │   ├── assessment.html     # 成绩测评
│   │   │   ├── summary.html        # 课后总结
│   │   │   └── group.html          # 群话术海报
│   │   ├── students.html           # 学员列表
│   │   ├── student-detail.html     # 学员详情
│   │   └── images/                 # 图片资源
│   ├── package.json                # 依赖配置
│   └── vercel.json                 # 部署配置
├── backups/                        # 备份目录
│   └── ui-polish-backup-20260822/  # UI优化前备份
└── .git/                           # Git 仓库
```

---

## 🔧 技术栈

- **前端**: HTML5 + CSS3 + Vanilla JavaScript
- **API**: Vercel Serverless Functions (Node.js)
- **部署**: Vercel (自动部署)
- **存储**: 内存 + 种子文件 (临时方案)
- **版本控制**: Git + GitHub

---

## 🚀 部署指南

### 环境要求

- Node.js 16+
- npm 或 yarn
- Git
- Vercel CLI (可选)

### 本地开发

```bash
# 1. 克隆仓库
cd D:\manfenhu-website

# 2. 安装依赖
cd youth-fitness-website
npm install

# 3. 本地预览
npx astro dev
# 访问 http://localhost:4321
```

### 生产部署

**方式一：Vercel CLI**
```bash
# 设置 token
export VERCEL_TOKEN="你的token"

# 部署到生产
npx vercel --prod --yes
```

**方式二：Git 推送自动部署**
```bash
git add .
git commit -m "更新内容"
git push origin master

# Vercel 自动检测并部署
```

### 环境变量

当前生产环境已配置：
- `BLOB_READ_WRITE_TOKEN`: Vercel Blob 存储 token
- `BLOB_STORE_ID`: Blob store ID
- 其他遗留 KV 变量（已弃用）

**注意**: Blob 持久化已启用（见下文"数据持久化"），种子文件仅作为无 token 时的本地回退。

---

## ⚠️ 重要注意事项

### 数据持久化（Vercel Blob）

**当前状态**: Vercel Blob 持久化（已启用）✅

- `api/data.js` 接入 `@vercel/blob`：整个 store 作为一个 JSON blob（`manfenhu-store.json`）读写
- 有 `BLOB_READ_WRITE_TOKEN` 时：每次写操作（学员创建 / 加测评记录 / 删除）自动原子覆盖写入 Blob；每个读请求从 Blob 拉最新，重启 / 重新部署不丢
- 无 token 时自动 fallback 到内存 + 种子文件（本地开发 / 诊断模式可用）

**影响**:
- ✅ 数据实时持久化（保存即落盘）
- ✅ 重新部署 / 冷启动不丢数据
- ⚠️ 极端并发时"最后写入者生效"（内部低频工具几乎不影响）

**环境变量**（Vercel 控制台已配置）:
- `BLOB_READ_WRITE_TOKEN`: Blob 读写 token
- `BLOB_STORE_ID`: Blob store ID

**诊断**: 生产环境访问 `/api/data?diag=1`，返回 `backend: "blob+seed"`、`persistent: true` 即表示 Blob 生效。

**备用方案**（未采用）: Upstash Redis / Supabase；如需迁移只需改 `api/data.js` 的 `loadStore` / `saveStore` 两个函数。

### 120分上限逻辑

**场景**: 国体测评（标准分满分100 + 附加分满分20 = 总分满分 120）

**处理**: API 自动钳制
```javascript
if (record.mode === 'guoti' && record.total > 120) {
  record.total = Math.min(120, Math.round(record.total * 10) / 10);
  record.max = 120;
}
```

**位置**: `api/data.js` 第 99-102 行

> ⚠️ 界面显示：结果头 / 图片报告 / 复制报告均按满分 120 展示（如 `94.5/120分`）。

### 缺录项目按 0 分计

**场景**: 输入成绩时部分项目未填

**处理**: 前端计算时缺录项目按 0 分计入（不再跳过），分母仍为该年级全部项目权重总和（国体恒为 100）或全部类别满分总和（9年级恒为 30）：
```javascript
/* computeGuoti：分母 = 全部项目权重总和（恒100），缺录按 0 分计 */
keys.forEach(function(key){ totalW += weights[key]; });
/* 未填项目：missing++; return; 但不减分母 */
```

**位置**: `public/tools/assessment.html` `computeGuoti` / `computeXianchang`

**结果头提示**: 存在缺录时副标题追加"· 缺录 M 项按 0 分计"。

### 7维度雷达图

**维度映射**:
```javascript
const DIM_MAP = {
  'bmi': '体态',
  'vital': '心肺',
  'run50': '速度',
  'sitreach': '柔韧',
  'longjump': '爆发',
  'power': '力量',
  'run800': '耐力',
  'run1000': '耐力'
};
```

**自动聚合**: 从 `items` 数组自动生成 `dimensions` 对象

---

## 📋 页面清单

| 页面 | 路径 | 功能 |
|------|------|------|
| 工具导航 | `/tools/` | 统一入口，老师只需收藏这一个 |
| 学员列表 | `/students.html` | 查看所有学员，批量导出 |
| 学员详情 | `/student-detail.html` | 单个学员档案，添加/查看记录 |
| 成绩测评 | `/tools/assessment.html` | 国体+现场评分，7维度分析 |
| 课后总结 | `/tools/summary.html` | 28项×10档智能生成 |
| 群话术海报 | `/tools/group.html` | 节气/节日/天气/品牌句海报 |

---

## 🎨 UI设计规范

### 品牌色

- **主色**: `#FF6B35` (满分虎橙)
- **辅助色**: `#FF8A4C` / `#F04E23`
- **背景渐变**: `linear-gradient(135deg, #FFF5F0 0%, #FFE8DD 100%)`

### 字体

```css
font-family: -apple-system, BlinkMacSystemFont, 
             "Segoe UI", "PingFang SC", 
             "Microsoft YaHei", sans-serif;
```

### 圆角

- 大卡片: `16-18px`
- 中按钮: `12-14px`
- 小标签: `8-10px`

### 交互

- 悬停: `translateY(-2px)` + 阴影加深
- 过渡: `transition: all 0.3s ease`

---

## 🔄 备份与回滚

### 备份位置

1. **Git Tag**: `backup-before-ui-polish`
   ```bash
   # 回滚到备份点
   git checkout backup-before-ui-polish
   ```

2. **本地备份**: `backups/ui-polish-backup-20260822/`
   - 包含所有主要页面
   - 可直接复制回 public 目录

3. **GitHub 仓库**: 完整历史记录
   ```bash
   # 查看提交历史
   git log --oneline

   # 回滚到指定提交
   git reset --hard <commit-id>
   ```

### 紧急回滚

```bash
# 1. 回到最后一次稳定提交
cd D:\manfenhu-website
git reset --hard 5efdbe1

# 2. 强制推送（慎用）
git push origin master --force

# 3. 重新部署
npx vercel --prod --yes
```

---

## 📊 数据导出格式

### 学员列表导出 (CSV)

```csv
学员ID,姓名,性别,年级,记录数,最后测评日期
s_1787418222875,张三,男,8,3,2026-08-22
```

### 单人详情导出 (CSV)

```csv
日期,年级,模式,总分,等级,BMI,肺活量,50米跑,...
2026-08-22,8,国体,94.5,优秀,96,93,95,...
```

---

## 🐛 已知问题与解决方案

### 1. 数据重置问题

**现象**: 重新部署后学员数据消失

**原因**: 当前使用内存+种子文件存储

**解决**: 
- 短期: 定期导出备份
- 长期: 接入真正的数据库

### 2. Vercel Blob 配置问题（已解决 ✅）

**现象**: Blob API 初始化失败

**状态**: ✅ 已启用（2026-08-25）

**原因**: 初次部署遇到 `list()` / `put()` API 兼容问题

**解决**: 重写 `api/data.js` 接入 `@vercel/blob`：`loadStore()`（list + fetch 读取）与 `saveStore()`（put 原子覆盖），并做无 token fallback。部署后访问 `/api/data?diag=1` 验证 `persistent: true` 即生效。

---

## 📞 联系方式

**项目维护**: AI Agent (Hermes/Kiro)  
**业务负责人**: 满分虎创始人  
**技术支持**: lx676 (GitHub: lx67621956-create)

---

## 📅 更新日志

### 2026-08-22 (v1.0)

**功能修复**:
- ✅ 100分越界限制（国体 >100 自动钳制为 100）
- ✅ 单项得分显示（详情页展开所有项目）
- ✅ 7维度雷达图修复（自动聚合维度数据）
- ✅ 导出功能（列表批量 + 详情单人 CSV）
- ✅ 数据持久化（种子文件方案）

**新增功能**:
- ✅ 统一工具导航页 (`/tools/`)
- ✅ 所有页面 UI 统一（品牌橙色 + 渐变背景）

**技术变更**:
- 从 Upstash KV（已失效）迁移到种子文件存储
- 添加 `@vercel/blob` 依赖（备用）
- 创建完整备份（Git tag + 本地文件）

---

### 2026-08-25 (v1.1)

**成绩测评优化**:
- ✅ 每个学科输入框下方显示评分参考（满分成绩 / 50分成绩 / 最低成绩，来自官方评分表）
- ✅ 缺录项目按 0 分计算（不再忽略或按已填比例折算），分母恒为全部权重/类别满分
- ✅ 国体满分按 120 展示（标准分100 + 附加分20；结果头/图片/复制报告同步），后端钳制上限改为 120
- ✅ BMI 身高体重输入区 UI 优化（单位标签、参考行紧凑化）
- ✅ 修复：保存档案时等级恒为 undefined（补 gradeOf 兜底）
- ✅ 修复：学员档案自动填充时年级/性别选择器高亮联动错误（选择器与 DOM 不匹配）

**数据持久化**:
- ✅ 启用 Vercel Blob 持久化：`api/data.js` 接入 `@vercel/blob`，整个 store 存为 `manfenhu-store.json`，写操作原子落盘、读请求实时拉取，重新部署不丢数据；无 token 自动回退内存+种子

**安全加固**:
- ✅ 清除仓库全部硬编码密钥（5 组），脚本改为环境变量读取
- ✅ 用 git filter-repo 重写历史移除密钥并 force push（提交哈希已变）
- ⚠️ 业务方选择不轮换密钥，残余风险见"🔐 安全注意事项"

---

## 🔐 安全注意事项

1. **API Token 保护**
   - `VERCEL_TOKEN` 不要提交到 Git，仅存 Vercel 控制台 / 本地环境变量
   - ⚠️ **2026-08-25 历史密钥事件**：仓库历史中曾硬编码 5 组密钥（VERCEL_TOKEN / IMG_API_KEY / SPARK_API_KEY / GPT88_API_KEY / ARK_API_KEY），已通过 `git filter-repo` 重写历史 + force push 清除，历史提交哈希已全部变化
   - ⚠️ **仍然存在残余风险**：仓库在公开期间被 fork/访问过的提交，GitHub 缓存可能仍含旧内容；且业务方选择**不轮换**这些密钥——在密钥作废前，泄密风险未彻底消除（尤其 VERCEL_TOKEN）。建议尽快到各平台控制台轮换
   - 所有脚本密钥已改为环境变量读取（见脚本头部注释）

2. **数据隐私**
   - 学员信息存储于 Vercel Blob（`manfenhu-store.json`）
   - 导出的 CSV 文件请妥善保管

3. **环境变量管理**
   - 生产环境变量仅通过 Vercel 控制台配置
   - 不要在代码中硬编码敏感信息；新增密钥一律走环境变量（`*.secret`/`*.pem` 已入 .gitignore）

---

## 🎯 未来优化方向

### 短期（1-2周）
1. ✅ Vercel Blob 持久化（已启用，见"数据持久化"）
2. 添加数据自动备份到飞书文档
3. 可选：如需关系型能力再评估 Upstash Redis / Supabase

### 中期（1-3个月）
1. 学员成绩趋势分析图表
2. 家长端小程序/H5 查看
3. 微信/飞书消息通知

### 长期（3个月+）
1. AI 训练计划推荐
2. 视频动作纠正
3. 多校区数据同步

---

## ✅ 交接确认清单

- [ ] 已阅读完整文档
- [ ] 已克隆代码仓库到本地
- [ ] 已验证本地开发环境可用
- [ ] 已访问线上所有页面并测试功能
- [ ] 已了解数据持久化限制
- [ ] 已知晓备份与回滚流程
- [ ] 已保存重要联系方式
- [ ] 已导出一次学员数据作为测试

---

**交接完成日期**: ____________  
**交接人签名**: ____________  
**接收人签名**: ____________

---

*本文档最后更新: 2026-08-25*  
*版本: 1.1*
